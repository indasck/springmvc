spring:
  application:
    name: MicroServiceGreet

  boot:
    admin:
      client:
        url: http://localhost:8022 # Spring Boot Admin server URL

server:
  port: 8024 # Correct port for your microservice (it should match the actual port of the service)

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # Eureka server URL

management:
  endpoints:
    web:
      exposure:
        include: '*' # Exposes all actuator endpoints
